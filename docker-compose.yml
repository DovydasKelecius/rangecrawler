services:
  # The RangeCrawler Broker Agent
  broker:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config.yaml:/app/config.yaml # Only mount the config
    environment:
      - PYTHONPATH=/app
      - GEMINI_API_KEY # Automatically pulls from your host shell environment

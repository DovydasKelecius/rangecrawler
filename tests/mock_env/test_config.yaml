broker:
  host: "0.0.0.0"
  # Use the service name defined in docker-compose.yml
  port_assignment_url: "http://mock-llm:8001/api/port"
  default_port: 8000

models:
  - id: "test-model"
    remote_url: "http://mock-llm:8001"
  - id: "gemini-test"
    remote_url: "http://mock-llm:8001"

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

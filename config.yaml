broker:
  host: "0.0.0.0" # Listen on all interfaces inside container
  port_assignment_url: null 
  default_port: 8005
  idle_timeout: 300
  check_interval: 60

models:
  - id: "gemini-2.5-flash"
    remote_url: "https://generativelanguage.googleapis.com/v1beta/openai/"

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

auth:
  gemini_api_key: "AIzaSyBvDDDBm1DSnAlU3oI_FGBQaOt869wvn58"
